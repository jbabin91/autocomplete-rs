name: Branch Cleanup

on:
  pull_request:
    types: [closed]
  schedule:
    # Run daily at midnight UTC
    - cron: '0 0 * * *'

permissions:
  contents: write

jobs:
  # Delete branches from PRs closed without merging
  closed-pr:
    name: Delete unmerged PR branch
    if: github.event_name == 'pull_request' && !github.event.pull_request.merged
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      # The branch name is attacker-controlled: anyone who can open a pull
      # request chooses it. Interpolated into the script it would run as shell in
      # a job holding `contents: write`, so it arrives as data.
      - name: Delete branch
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          BRANCH: ${{ github.event.pull_request.head.ref }}
        run: gh api "repos/${REPO}/git/refs/heads/${BRANCH}" -X DELETE

  # Remove branches with no activity for 30+ days
  stale:
    name: Remove stale branches
    if: github.event_name == 'schedule'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Remove stale branches
        uses: fpicalausa/remove-stale-branches@9b829bc2975ade0c61e64e9613def53ec0732440 # v2.6.1
        with:
          days-before-branch-stale: 30
          days-before-branch-delete: 7
          exempt-branches-regex: ^(main|beads-sync)$
          ignore-branches-with-open-prs: true
          operations-per-run: 10
