# release.yml is generated by cargo-dist (`dist generate-ci`) — a fix applied
# here is erased by the next regeneration, and the findings are style-level
# shellcheck notes in dist's own scaffolding.
paths:
  .github/workflows/release.yml:
    ignore:
      - '.+'
