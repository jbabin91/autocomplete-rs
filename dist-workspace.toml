[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.30.3"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell", "homebrew"]
# Target platforms to build apps for (Rust target-triple syntax)
targets = [
  "aarch64-apple-darwin",
  "aarch64-unknown-linux-gnu",
  "x86_64-apple-darwin",
  "x86_64-unknown-linux-gnu",
]
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Where to host releases
hosting = "github"
# Whether to install an updater program
install-updater = false
# Homebrew tap repository
tap = "jbabin91/homebrew-tap"
# Publish Homebrew formula on release
publish-jobs = ["homebrew"]
# Don't run on PRs (CI handles PR validation)
pr-run-mode = "skip"
# Enable GitHub artifact attestations
github-attestations = true
# Allow Renovate to update action SHAs directly in release.yml
# Remove temporarily when regenerating: dist generate-ci
allow-dirty = ["ci"]

# Pin GitHub Actions to specific commit SHAs for supply chain security
[dist.github-action-commits]
"actions/checkout" = "0c366fd6a839edf440554fa01a7085ccba70ac98"
"actions/upload-artifact" = "b7c566a772e6b6bfb58ed0dc250532a479d7789f"
"actions/download-artifact" = "37930b1c2abaa49bbe596cd826c3c89aef350131"
"actions/attest-build-provenance" = "c44148e5bf178192efd8947e07a0d439a356c60b"
